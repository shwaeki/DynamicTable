{{--
    The empty slot: what to do when there is nothing here yet.

    Rendered only when the table is genuinely empty. Filter this table down to
    nothing and you get "Nothing matches these filters" and a Clear filters
    button instead — a different situation, with a different answer.
--}}
<a class="dynamic-table-button" href="{{ route('examples.show', 'inline-create') }}">
    Add the first order
</a>
