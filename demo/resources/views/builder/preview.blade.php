{{--
    The live table, as a fragment.

    The same directive the documentation tells you to write; the builder just
    hands it an instance it configured from the current selection.
--}}
@dynamicTable($table)
